# Base URL of the AwsCertPrep.Api project. Copy to .env.development to override.
# Leave it empty (or omit the file) for a production build served from the API's own origin,
# or from behind a proxy that forwards /api to the API.
VITE_API_BASE_URL=http://localhost:5176
