# Normalise to LF in the repository, check out native. Without this, editing on Windows rewrites
# whole files as CRLF and every diff looks like a rewrite.
* text=auto

# Binary and already-normalised assets: never touch these.
*.pdf binary
*.png binary
*.jpg binary
*.ico binary
*.svg text eol=lf
